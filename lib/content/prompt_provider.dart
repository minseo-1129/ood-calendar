String promptForDate(DateTime date) {
  // Kept behind one function so daily prompt rotation can be added later
  // without changing the screen or persistence layers.
  return '오늘 이상하게 기억나는 것 하나';
}
